{
  "name": "urban-freeway-cap-feasibility",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run client:dev\" \"npm run server:dev\"",
    "client:dev": "vite",
    "server:dev": "cd server && npm run server:dev",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run build && cd server && npm start"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.27.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "vite": "^6.0.1",
    "concurrently": "^8.2.2"
  }
}
